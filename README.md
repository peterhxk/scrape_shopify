# scrape_shopify

Detects internship openings from shopify in 5 minutes interval (can be adjusted), sends an email to personal email upon detection.

Make sure to set up .env with https://myaccount.google.com/apppasswords
Pip install -r requirements.txt